[package]
org = "gha-indie-worker"
name = "gha-indie-worker-api-server"
kind = "server"
version = "0.1.0"
description = "JSON API server for GHA Indie Worker"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/gha-indie-worker/gha-indie-worker-api-server.rs"

[targets.rust]
dir = "."
adapter = "rust"

[install]
adapter = "none"
